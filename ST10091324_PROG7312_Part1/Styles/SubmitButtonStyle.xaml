<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!--
        The code for the styling below was modified and implemented from a YouTube video
        Titled: C# WPF UI | How to Design Flat Data Table Dashboard in WPF (Customize Datagrid)
        Link: https://www.youtube.com/watch?v=mlmyFXJy8gQ
    -->
    <Style x:Name="submitBtnForm" TargetType="Button">
        <Setter Property="Background" Value="#45b3e7"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="MinHeight" Value="45"/>
        <Setter Property="FontSize" Value="23"/>


        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}" CornerRadius="7">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#6ac2eb"/>
                            <Setter Property="Foreground" Value="#ecf7fc"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>

    </Style>
   
</ResourceDictionary>